apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopez.fullname" . }}
  labels:
    {{- include "shopez.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "shopez.selectorLabels" . | nindent 4 }}