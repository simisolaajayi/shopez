apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopez.fullname" . }}-test-connection"
  labels:
    {{- include "shopez.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopez.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
